#include <stdio.h>
#include <math.h>

int main()
{
	int t;
	scanf("%d",&t);
	int n,k,i,j;
	long long int a[n];
	while(t--)
	{
		scanf("%d %d",&n,&k);
		printf("n = %d and k = %d\n",n,k);
		for(i=0;i<n;i++)
		scanf("%lld",&a[i]);
		

		

	}
	return 0;
}